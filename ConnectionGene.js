class ConnectionGene {
	constructor(inNode, outNode, weight, disabled, innovation) {
		this.inNode = inNode;
		this.outNode = outNode;
		this.weight = weight;
		this.disabled = disabled;
		this.innovation = innovation;
	}
}